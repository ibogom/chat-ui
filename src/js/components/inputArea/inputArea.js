import React from 'react';
import PropTypes from 'prop-types';

export default class InputArea extends React.Component {

    static propTypes = {

    };

    render() {
        return (<div className="user-input-area"/>);
    }
};